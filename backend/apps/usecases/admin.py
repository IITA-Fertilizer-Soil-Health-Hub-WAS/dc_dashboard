from __future__ import annotations

# Use cases and their config (forms, field mappings, event schedule, crops,
# trials, stages) are managed in the in-app console (/manage/...), including the
# Validate / Sync now / Export YAML actions. Not registered in the Django admin
# to avoid duplicating the same screens.
