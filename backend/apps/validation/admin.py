from __future__ import annotations

# Validation rules and flags are managed/viewed in the in-app console
# (/manage/validation-rules/, /manage/validation-flags/). Not registered in the
# Django admin to avoid duplicating the same screens.
