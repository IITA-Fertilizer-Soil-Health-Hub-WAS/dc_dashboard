from __future__ import annotations

# Users are managed in the in-app console (/manage/users/), with Approve /
# Deactivate actions there — so the User model is intentionally NOT registered in
# the Django admin to avoid duplicating the same screens.
